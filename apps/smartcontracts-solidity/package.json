{
  "name": "smartcontracts-solidity",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "pnpm truffle compile",
    "deploy": "pnpm truffle migrate --reset --network matic",
    "verify:catalog": "pnpm truffle run verify SuperloudCatalog --network matic",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
 "license": "GPL-3.0",
  "devDependencies": {
    "@truffle/hdwallet-provider": "^2.1.6",
    "dotenv": "^16.0.3",
    "truffle": "^5.7.5",
    "truffle-plugin-verify": "^0.6.1"
  }
}
